from collections import deque
a=list(map(int,input().split()))
k=int(input())
d=deque()
res=[]
for i in range(len(a)):
    while d and d[0]<=i-k:
        d.popleft()
    while d and a[d[-1]]<=a[i]:
        d.pop()
    d.append(i)
    if i>=k-1:
        res.append(a[d[0]])
print(res)

def solve(b):
    for i in range(9):
        for j in range(9):
            if b[i][j]==0:
                for k in range(1,10):
                    if all(k!=b[i][x] for x in range(9)) and all(k!=b[x][j] for x in range(9)) and all(k!=b[i//3*3+x][j//3*3+y] for x in range(3) for y in range(3)):
                        b[i][j]=k
                        if solve(b):return True
                        b[i][j]=0
                return False
    return True
